name = "cf-graphql-proxy"
main = "cf-graphql-proxy.js"
compatibility_date = "2024-11-01"

# Routes: map the same-origin /api/cf/* path on each delivery host to this
# worker. Requires the delivery domain to be proxied through Cloudflare (BYO
# CDN). Add the production apex/custom domain route once live.
#
# [[routes]]
# pattern = "main--kotakbank--xeragobiz.aem.live/api/cf/*"
# zone_name = "<your-cloudflare-zone>"
#
# [[routes]]
# pattern = "main--kotakbank--xeragobiz.aem.page/api/cf/*"
# zone_name = "<your-cloudflare-zone>"
